import { Container, Typography } from "@mui/material";

export const Home = () => {
  return (
    <Container>
      <Typography variant="h1">ReliefHub</Typography>
      <Typography variant="h2">Localized Disaster Relief Platform</Typography>
    </Container>
  );
};
